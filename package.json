{
  "name": "node-duckdb",
  "version": "1.0.0",
  "main": "src/hello.js",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./build",
    "build:native": "rimraf build && cmake-js compile",
    "build:ts": "rimraf dist && tsc",
    "build": "yarn build:native && yarn build:ts",
    "build:test:watch": "nodemon --exec 'yarn build && yarn jest --watchAll'",
    "test": "jest"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "^2.0.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "cmake-js": "^6.1.0",
    "jest": "^26.2.2",
    "nodemon": "^2.0.4",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.3"
  },
  "engines": {
    "node": ">= 12.11.0"
  }
}
